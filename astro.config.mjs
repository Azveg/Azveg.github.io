// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Azveg.github.io', // Замените на свой
  base: '/', // Корневая папка
});
